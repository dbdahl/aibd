#!/bin/bash

set -e

cd $(dirname $(readlink -f "$0"))/..

FILENAME=tmp/aibd2-JARs.zip
mkdir -p $(dirname $FILENAME)
rm -f $FILENAME

cd R/aibd2
zip -r ../../$FILENAME inst java
cd ../..

rsync --chmod=644 $FILENAME dahl.byu.edu:public

